import logo from "/logo.jpeg";
const Header = () => {
    return(
        <header>
              {
                <img src = {logo} alt="logo" />

                 }
        </header> )
}

export default Header;

